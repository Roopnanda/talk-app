<!--
  This project doesn't include a full android/ folder — that gets
  generated by running `flutter create .` (see README step 1). Once it
  exists, merge these lines into android/app/src/main/AndroidManifest.xml.
-->

<!-- Permissions: only what the app actually uses -->
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.RECORD_AUDIO" />
<uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
<uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
<uses-permission android:name="android.permission.WAKE_LOCK" />
<uses-permission android:name="com.google.android.gms.permission.AD_ID" />

<application>
    <!-- AdMob: required app ID meta-data tag. Missing/malformed = hard
         crash on launch, per Google's own docs — this is Google's public
         sample App ID, safe for testing, matches the test ad unit IDs
         already used in ads_service.dart. Swap for your real AdMob App ID
         (from the AdMob console, not an ad unit ID) before release. -->
    <meta-data
        android:name="com.google.android.gms.ads.APPLICATION_ID"
        android:value="ca-app-pub-3940256099942544~3347511713" />

    <!-- Meta Audience Network: no manifest entry required beyond internet
         permission above; app ID/placement IDs are passed in Dart code. -->
</application>
